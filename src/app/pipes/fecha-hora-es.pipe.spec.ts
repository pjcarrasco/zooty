import { FechaHoraEsPipe } from './fecha-hora-es.pipe';

describe('fechaHora', () => {
  it('create an instance', () => {
    const pipe = new FechaHoraEsPipe();
    expect(pipe).toBeTruthy();
  });
});
